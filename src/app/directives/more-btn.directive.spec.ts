import { MoreBtnDirective } from './more-btn.directive';

describe('MoreBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new MoreBtnDirective();
    expect(directive).toBeTruthy();
  });
});
